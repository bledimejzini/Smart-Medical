# Database URL
# For Local Development: Use SQLite
# DATABASE_URL="file:./dev.db"

# For Vercel Postgres (Production): Automatically provided by Vercel
# DATABASE_URL and DIRECT_URL are set automatically when you add Vercel Postgres
# Example format: postgresql://user:password@host:5432/database?pgbouncer=true
# DIRECT_URL is for migrations (without pooling)

# For local development, you can also use PostgreSQL:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vitanet_dev?schema=public"
# DIRECT_URL is only needed for Vercel Postgres pooling

# NextAuth Configuration
# IMPORTANT: Generate a secure secret for production
# Run: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-replace-with-secure-random-string"

# MQTT Broker (for Raspberry Pi communication)
MQTT_BROKER_URL="mqtt://localhost:1883"
MQTT_USERNAME=""
MQTT_PASSWORD=""

# Optional: For Vercel deployment
# NEXTAUTH_URL="https://your-app.vercel.app"
# DATABASE_URL="postgresql://user:password@host:5432/vitanet_production"
